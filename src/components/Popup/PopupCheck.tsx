import React from "react";
import { CheckBox } from '@/src/components/Popup/styled';

interface PopupCheckProps {

};
export const PopupCheck = ({}: PopupCheckProps) => {
    return (
        <CheckBox />
    );
};